# 標準仮想PC 自動起動ツールの依存ライブラリ
# （初回起動時に自動でインストールされます）

pyautogui>=0.9.54     # 画面操作・キーボード入力の自動化
opencv-python>=4.8.0  # 画像認識（テンプレートマッチングの精度向上）
pillow>=10.0.0        # スクリーンショット処理（pyautogui が利用）
keyring>=24.0.0       # OSの資格情報ストアへの安全なパスワード保存
